{
  "name": "monica-sdk-js",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "core",
    "node",
    "cloudflare",
    "next"
  ],
  "scripts": {
    "check": "bun run check:core && bun run check:node && bun run check:cloudflare && bun run check:next && bun run check:tooling",
    "check:core": "bun run --filter @ah-monica/core typecheck && bun run --filter @ah-monica/core test && bun run --filter @ah-monica/core pack:check",
    "check:node": "bun run --filter @ah-monica/node typecheck && bun run --filter @ah-monica/node test && bun run --filter @ah-monica/node pack:check && bun run --filter @ah-monica/node test:runtime",
    "check:cloudflare": "bun run --filter @ah-monica/cloudflare typecheck && bun run --filter @ah-monica/cloudflare test && bun run --filter @ah-monica/cloudflare pack:check && bun run --filter @ah-monica/cloudflare test:runtime",
    "check:next": "bun run --filter @ah-monica/next typecheck && bun run --filter @ah-monica/next test && bun run --filter @ah-monica/next pack:check && bun run --filter @ah-monica/next test:runtime && bun run --filter @ah-monica/next test:next-build",
    "check:tooling": "bun test tooling",
    "test:contract": "bun run --filter @ah-monica/next build:dependencies && bun test contract.test",
    "version": "bun run tooling/set-version.ts",
    "spec:sync": "bun run tooling/spec-sync.ts",
    "spec:check": "bun run tooling/spec-sync.ts --check"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
